---
description: GitHub 操作严禁将 cursoragent 或其他方记为 contributor
alwaysApply: true
---

# GitHub：禁止添加 Agent / 其他 contributor

在进行任何 GitHub 相关操作时（包括但不限于：`git commit`、`git push`、`gh repo create`、创建/合并 PR、GitHub API 写文件等）：

- **严禁**将 `cursoragent`（或 Cursor Agent / `cursor-agent` 等变体）设为 commit author、committer，或以任何形式记为 contributor。
- **严禁**主动添加、邀请或配置任何其他 contributor / collaborator（除非用户在当次请求中明确要求指定某人）。
- 提交时使用仓库已有的本地 git 用户身份（用户自己的 name/email）；不要覆盖、伪造或改写 author/committer 为 Agent。
- **不要**修改 git config 来写入 Agent 身份；也不要通过 `git commit --author` 等方式挂上 Agent 或其他无关贡献者。
